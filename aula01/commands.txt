git init = inicia o repositorio;

git status = mostra como estar as operacoes;

git add = adiciona o arquivos ou a pasta, saindo de untracked para unmodified;

git add . = adiciona todos os arquivos e pastas;

git commit -m = torna rastreavel o arquivo ou pasta, junto com uma mensagem;